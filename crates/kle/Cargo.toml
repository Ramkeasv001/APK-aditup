[package]
name = "aditup-kle"
version = "0.1.0"
description = "The Self-Learning Engine (Module 9): staging, clustering, feedback-driven weight adjustment, and history logging."
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
aditup-db = { path = "../db" }
aditup-scoring-engine = { path = "../scoring-engine" }
thiserror = "2"
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
